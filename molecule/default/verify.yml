---
- name: Verify
  hosts: all
  become: false
  gather_facts: false

  tasks:
    - name: Run checkmk
      ansible.builtin.shell:
        cmd: omd version > omd.version
      args:
        creates: omd.version
